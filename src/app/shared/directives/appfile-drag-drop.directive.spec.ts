import { AppfileDragDropDirective } from './appfile-drag-drop.directive';

describe('AppfileDragDropDirective', () => {
  it('should create an instance', () => {
    const directive = new AppfileDragDropDirective();
    expect(directive).toBeTruthy();
  });
});
